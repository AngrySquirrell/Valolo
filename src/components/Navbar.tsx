import { useLocalStorage } from '@mantine/hooks';
import React from 'react';

const Navbar = () => {
    return (
        <div>
            Belle navbar
        </div>
    );
};

export default Navbar;